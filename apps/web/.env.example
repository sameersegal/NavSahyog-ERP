# Clerk publishable key — required by main.tsx ClerkProvider.
# Get yours from https://dashboard.clerk.com (it's the pk_test_… value).
# Vite-only env vars must be prefixed VITE_ to be exposed to the client.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

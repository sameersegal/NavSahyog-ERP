node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/
coverage/
mvp/recordings/
db/seed-clerk-link.sql

# Claude Code local session state — settings.local.json, agents/, todos/, etc.
# (`.claude-plugin/` is tracked for the plugin manifest and is unaffected.)
.claude/
